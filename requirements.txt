# Fast-dLLM Compatible Requirements
# Resolves conflicts with vllm and xformers

# Core dependencies - PyTorch 2.8.0 (compromise between Fast-dLLM and vllm)
torch>=2.9.1
torchvision>=0.24.1
torchaudio>=2.9.1

# Transformers and HuggingFace
transformers>=4.57.6
huggingface-hub>=0.34.0,<1.0
tokenizers>0.21.4
accelerate==0.34.2

# Datasets
datasets==2.14.6
fsspec>=2023.1.0,<=2023.10.0
pyarrow>=8.0.0,<15.0.0

# Evaluation
evaluate==0.4.0

# vLLM for optimized inference
vllm==0.14.0

# Utilities
numpy<2.0
scipy
tqdm
pandas

# Optional: xformers (if needed)
# xformers==0.0.32.post1

# Additional Fast-dLLM dependencies
sentencepiece
einops>=0.6.1
ipykernel
dotenv

# CRITICAL for Fast-dLLM speedup - Flash Attention
# Install with: pip install flash-attn --no-build-isolation
# Requires CUDA 11.6+ and PyTorch 2.0+
flash-attn>=2.5.0